import React, { useEffect, useRef, useState } from 'react';

export default function SmallMap(props) {
//   const smallMap = useRef();
// const [container] = useState(null)
  useEffect(() => {
    //  const { current } = smallMap;
   
    // console.log(document.getElementById('dagre-graph'))
    // document.getElementById('small-map').innerHTML = document.getElementById('dagre-graph').innerHTML
  })
  return (
    <div id={'small-map'}>
    </div>
  );
}
